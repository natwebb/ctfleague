- if current_user == @user
  %h1 Your Profile
- else
  %h1 #{@user.email}'s Profile

- if current_user == @user
  %h2 Your Leagues
  - @user.leagues.each do |league|
    .league
      %h3= league.name
      %p= 'Commissioner: ' + league.commissioner.username
